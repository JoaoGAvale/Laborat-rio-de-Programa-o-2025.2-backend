Para rodar o backend, use os seguintes comandos no seu terminal:

cd Laborat-rio-de-Programa-o-2025.2-backend
python -m venv venv
.\venv\Scripts\activate (powershell)
ou 
source ./venv/Scripts/activate (git bash)
pip install -r requirements.txt
flask run
